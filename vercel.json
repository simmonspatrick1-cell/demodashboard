{
  "buildCommand": "cd netsuite-dashboard && npm install && npm run build",
  "outputDirectory": "netsuite-dashboard/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
