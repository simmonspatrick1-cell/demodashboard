{
  "version": 2,
  "buildCommand": "cd netsuite-dashboard && npm ci && CI=false npm run build",
  "outputDirectory": "netsuite-dashboard/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
