{
  "buildCommand": "cd netsuite-dashboard && npm install && CI=false npm run build",
  "outputDirectory": "netsuite-dashboard/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
